# ArmorControl 开发里程碑

本文件是产品完成前的执行清单。每个里程碑必须以可重复验证的证据结束，不以“代码已经写完”代替验收。

当前状态：M1–M5 已完成实现与无游戏进程回归；M6 已完成控制、安全、发布资料、真实 KSP 加载与帧时间基准，尚待选定存档/载具后的真实飞行和实体多设备触控验收。

## M1 — 运行基础与实时链路

- 独立 `ArmorControl.dll`，使用 KSP 1.12.5 的 .NET 4.x Runtime 可加载、且本机构建工具链可重复编译的 `net461`。
- KSP Addon 通过原生 ApplicationLauncher 工具栏面板负责服务启动、停止、端口设置与场景安全生命周期。
- 自带 TCP HTTP/WebSocket 服务，不依赖 ASP.NET、kRPC 或外部 Web 服务器。
- HTTP 提供现有网页、`/health` 与协议能力信息。
- `/ws/realtime` 支持多个浏览器、最新遥测广播、可靠命令回执和心跳。
- 网络线程不访问 Unity/KSP 对象；KSP 主线程通过不可变快照与全局 FIFO 命令队列交换数据。
- 验收：编译通过；协议、静态文件和 WebSocket 帧测试通过；进入游戏后的双设备连接测试通过。

## M2 — 核心 KSP 遥测协议

- 版本化消息封装、能力协商、单位与不可用值规范。
- `settings.cfg` 提供服务启停、监听地址/端口及 1–60 Hz 快速、1–20 Hz 常规采样配置。
- 快速遥测：姿态、速度、海拔、航向、油门、推力、G 力和动压。
- 常规遥测：轨道、资源摘要、目标、场景和载具身份。
- 事件：换船、分级、对接、解体、目标与场景变化。
- 验收：记录采样/序列化耗时；单客户端稳定 30 Hz，配置允许 60 Hz。

## M3 — 真实网页客户端

- 移除原型的随机模拟数据，接入 WebSocket 状态存储。
- 自动重连、陈旧状态、断线冻结、协议版本错误和服务端错误界面。
- 多客户端独立订阅与慢客户端背压隔离。
- 验收：桌面、平板与手机同时连接，页面状态保持一致且互不阻塞。

## M4 — 信息与记录模块

- 完整 Flight Pannel 数据。
- Vessel 零件、级段、温度、资源与模块状态。
- 当前载具成员和舱位。
- Flight Recorder 历史、增量、降采样与 CSV。
- 验收：逐项对照当前 MechJeb 配置，无缺项；十分钟记录不全量重传。

实现基线：Flight Pannel 公开 API 同源快照；当前载具成员与无上限零件结构按事件失效、按需获取；Flight Recorder 初始历史一次获取、后续 5 Hz 增量推送并支持 18 列 CSV。

## M5 — MechJeb 规划与自动控制

- 每种 Maneuver Operation 独立 schema、前置条件、预览和节点操作。
- 执行时机直接配置 MechJeb `TimeSelector`；完整节点队列来自 KSP 权威状态，新建/替换在正确轨道分支求解。
- Advanced Transfer 异步计算与 Porkchop Selection。
- 自动着陆、Smart A.S.S. 与自动对接。
- 验收：网页与 MechJeb 状态双向一致，失败和不可用状态有明确回执。

## M6 — 控制、性能与发布

- 灯光、起落架、刹车、SAS、RCS、动作组 01–10、太阳能板和天线。
- 全局 FIFO 离散命令；连续输入合并、限幅与断线归零。
- 危险操作确认、访问令牌、路径安全、包大小和帧预算监测。
- 安装包、配置说明、故障诊断与回归测试。
- 一键发布验收脚本禁止模拟遥测回归；安装 ZIP 保留可直接解压的 `GameData` 路径。
- 验收：多设备压力测试、断线安全测试、KSP 帧时间基准和完整功能清单全部通过。
