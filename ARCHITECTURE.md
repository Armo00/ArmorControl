# ArmorControl 推荐方案基线

状态：供高保真设计和后续实现共同使用的首版基线。  
前置调查：见 `INVESTIGATION.md`。

## 1. 产品目标

ArmorControl 是由 KSP 游戏进程内自有 DLL 提供服务、供局域网手机和平板访问的载具遥测与基础控制网页。

首版必须做到：

- 完整包含当前 MechJeb `Flight Pannel` 的全部 45 个遥测项和 3 个操作项，但重新组织信息，不复刻原窗口布局。
- 提供独立 Flight Recorder 页面，兼容读取 MechJeb 当前历史，同时允许 ArmorControl 建立更高频增强记录。
- 提供适合触控的结构化 Vessel View，展示零件树、级段、温度、资源和状态。
- 提供有限、明确、可撤销的基础控制，并在断线或失去控制权时自动进入安全状态。

## 2. 系统边界

### 游戏内 ArmorControl DLL

- 在 Unity/KSP 主线程安全地采集 Vessel、Orbit、Part 和 MechJeb 状态。
- 根据依赖是否存在加载 MechJeb、VesselView、RemoteTech 适配器。
- 在后台线程承载 HTTP 静态文件服务和 WebSocket 连接。
- 主线程只生成不可变快照并接收待执行命令；网络线程不得直接持有或遍历 KSP 对象。
- 提供本机配置：监听地址、端口、访问令牌、允许的控制范围和频率上限。

### 浏览器客户端

- 使用同一份响应式界面覆盖手机、平板和桌面浏览器。
- 不直接依赖 kRPC 协议；只消费 ArmorControl 的稳定网页协议。
- 使用最近两个快速样本插值，以设备的 60/90/120 Hz 屏幕刷新率绘制仪表。
- 缓存 Flight Recorder 历史，并只接收新增点或按视窗降采样后的数据。

### kRPC

- 是可选的数据源和交叉验证通路，不是浏览器或 ArmorControl 的硬依赖。
- 现有 `fixKRPC.dll` 的验证状态不影响只读网页，但连续控制轴在去重修订完成验证前不进入默认控制集。

## 3. 页面结构

### Flight

- 首屏显示姿态、海拔、速度、垂直速度、航向、推力、TWR、动压、G 力、油门和连接状态。
- 本页聚焦飞行仪表和高频核心值，不承担完整数据墙职责。

### Flight Pannel

- 宽屏专用信息总览，与 Flight 仪表页分离。
- 45 个 MechJeb 遥测字段全部呈现；界面为提高可读性将原配置中的第三段拆成“性能（15）”和“载具（6）”，另有轨道（8）、地表（7）与飞行（9）。3 个操作项提供只读状态回显。
- 飞行段包含 AoA、Angle of Sideslip、大气压力、当前 Biome、飞行动压、Suicide Burn 倒计时、撞击倒计时和两个目标距离，并复显 Heading、地表速度、水平地表速度、垂直速度四个常用实时量。
- 近拱点高度小于 0 时使用红色告警，非负时恢复普通数值颜色。
- 使用紧凑多列数据墙；宽屏无需切换标签即可完成全局扫视，小屏自动折叠为单列。
- 每个数据域显示自身刷新等级，避免将 2 Hz 仿真值误认为 30–60 Hz 实时值。

### Recorder

- 时间/下行距离横轴切换。
- 支持 MechJeb 的 18 种 recordType、级段标线、自动缩放、Mark、暂停、清空和 CSV 下载。
- MechJeb 兼容模式显示其现有 5 Hz 历史；增强模式默认 20 Hz，可选择每物理 tick 采样。
- 历史采用环形缓冲；显示端按像素宽度降采样，不全量重复传输。

### Maneuver Planner

- 参照本机 MechJeb `MechJebModuleManeuverPlanner`，覆盖圆化、变更拱点、倾角/LAN、霍曼转移、拦截、匹配轨道/速度、共振轨道、卫星返回和星际转移等节点操作。
- 每个 Operation 使用独立参数 schema；前端切换机动类型时替换字段、可用执行时机、前置条件、单位与结果摘要，不复用一套静态表单。Operation 实例的参数状态应在页面切换后保留，与 MechJeb 的行为一致。
- 支持执行时机、误差、预留时间、自动加速、燃烧时间计算，以及创建、修改、执行和移除节点。
- 所有节点写入和执行命令都进入全局 FIFO 队列，并以服务器回执作为最终结果。

#### Advanced Transfer / Porkchop Selection

- Porkchop 图横轴为出发日期、纵轴为转移时长、颜色为总 Δv；支持鼠标或触控点击/拖动选点，以及“最低 Δv”“尽快出发”和范围重置。
- DLL/MechJeb 侧异步计算转移矩阵，HTTP/WebSocket 线程不得直接执行求解。状态机为 `idle → computing(progress) → ready | failed | cancelled`，改变目标、捕获燃烧或范围时取消旧任务并重算。
- 浏览器只接收降采样后的颜色纹理/数值网格、坐标范围、最佳点和当前选择的精确解；不把完整高密度矩阵按遥测帧率重复下发。选点只回传样本索引或出发时间与时长，再由游戏侧完成精确优化并创建节点。
- “包括捕获燃烧”和目标近拱点会改变成本函数；未完成计算、计算失败、无效选点、目标不满足星际转移前置条件时禁用创建节点，并显示明确原因。

### Autopilot

- 自动着陆参照 `MechJebModuleLandingGuidance`：目标坐标、落点选择、着陆架/降落伞/RCS、轨迹显示、预测结果和启动/终止。
- Smart A.S.S. 参照 `MechJebModuleSmartASS`：轨道、地面、目标、高级和自动参考系，以及顺/逆向、法向、径向、相对速度、目标方向和偏移角。
- 自动对接参照 `MechJebModuleDockingAutopilot`：目标/控制对接口校验、速度限制、起始/安全距离、X/Y/Z 误差、轴线距离和自动驾驶状态。
- 同一时间只允许一个 MechJeb 自动控制模块持有控制；切换模块时先明确终止旧模块。

### Vessel

- 默认用 Canvas/SVG 绘制可缩放、旋转和点选的结构化载具示意图。
- 支持状态、级段、温度和资源着色。
- 右侧或底部检查器显示所选零件名称、父子关系、级段、质量、温度、资源和模块状态。
- 原版 Vessel View RenderTexture 可作为后续低帧率辅助模式，不作为首版唯一视图。

### Crew

- 只展示当前载具实际搭载的成员，不作为全局宇航员名册。
- 列出宇航员姓名、职业、等级、所属舱段、座位和当前状态，并汇总各舱段占用情况。

### Control

- 首版开放灯光、起落架、刹车、SAS、RCS、动作组 1–10、太阳能板和天线。
- 分级、自动分级等危险操作长按确认；普通状态开关直接切换并显示服务器回执。
- 连续油门和姿态控制待 FlyByWire 回调去重修订完成验证后启用。

## 4. 更新与绘制频率

| 数据层 | 默认值 | 可配置目标 | 说明 |
|---|---:|---:|---|
| 浏览器视觉动画 | 屏幕刷新率 | 60/90/120 Hz | 使用样本插值，不要求游戏生成同频物理状态 |
| 快速遥测 | 30 Hz | 最高 60 Hz 起步 | 姿态、速度、油门、推力、G 力、动压等小型快照 |
| 控制输入 | 每显示帧合并 | 每物理 tick 应用一次 | 服务端只保留最新输入；超时自动释放 |
| 常规遥测 | 5–10 Hz | 2–20 Hz | 轨道、资源汇总、温度摘要、目标状态 |
| MechJeb 级段仿真 | 1–2 Hz | 状态变脏时立即更新 | 避免每帧触发燃流模拟 |
| Flight Recorder | 20 Hz | MechJeb 5 Hz / 物理 tick | 增强记录与兼容历史分开 |
| 零件结构 | 事件触发 | 低频哈希校验 | 换船、对接、解体、分级时重建 |
| 原版 Vessel View 图像 | 5–15 FPS | 实测后调整 | GPU 回读和编码路径必须限频 |

60 Hz 不是系统硬上限。上限由实际物理 tick、新鲜数据量、主线程采样耗时、序列化分配、客户端数量和移动设备绘制能力共同决定。实现阶段应加入耗时与包大小统计，再根据帧预算动态降级慢速任务。

## 5. 数据通道

### 快速状态帧

固定结构、小尺寸、单调序号和游戏时间戳。包含姿态四元数/欧拉角、三轴速度、海拔、航向、油门、推力、G 力、动压和连接状态。

### 常规状态帧

按分组发送轨道、推进、气动、载具、着陆和目标数据。每组具有独立版本号；未变化的组不重复发送。

### 结构事件

换船、零件树变化、级段、对接、解体、目标变化和 RemoteTech 链路变化使用事件消息。

### Recorder 流

初次连接传输视窗所需的降采样历史，随后只推送新增点。客户端改变时间范围或缩放时再请求对应分辨率。

## 6. 控制安全

- 不设置 Observer、Operator、Admin 等多用户角色，也不要求客户端申请控制租约；所有已通过连接校验的设备均可发送控制命令。
- 来自手机、平板等所有客户端的离散命令写入同一个全局 FIFO 队列，按服务端接收顺序在 KSP 主线程逐条执行。每条执行结果广播给全部设备。
- 连续轴输入也按接收顺序处理，但在尚未进入物理 tick 前，同一客户端同一控制轴的旧采样允许由更新采样覆盖，避免网络卡顿后回放过时操纵动作；断线、页面隐藏或超时后该客户端的轴输入立即归零。
- 危险操作包含命令 ID、载具 ID、当前 stage 和短时确认令牌，防止重放和换船误触。
- UI 的“已按下”不代表成功；只有收到游戏线程回执后才改变确认状态。
- RemoteTech 可用时，同时展示连接、许可与信号延迟；是否模拟延迟由后续需求确认。

### 多设备连接

- 服务端只采集一次 KSP 状态，再广播给全部连接；不会按客户端重复读取载具或 MechJeb 数据。
- 每个客户端可独立订阅所需页面的数据组和刷新频率。某个慢客户端只丢弃自己的过期实时帧，不阻塞其他客户端。
- 不做控制权抢占或等待队列；“FIFO”约束的是实际控制命令的执行顺序。服务端保留 `clientId + commandId + sequence`，仅用于排序、去重、回执和诊断，不代表用户角色。

## 7. 断线和降级

- 超过两个快速帧周期未收到数据时显示延迟；超过一秒显示陈旧；连接断开时冻结数值并标记最后更新时间。
- 断线客户端不能继续发送操作；它留下的连续输入归零，危险确认令牌作废，其他已连接设备不受影响。
- 未安装 MechJeb 时保留页面结构，能由 KSP 数据重算的字段继续显示，MechJeb 特有字段明确标记不可用。
- 未安装 VesselView 不影响结构化 Vessel 页面。

## 8. 验收指标

- 独立 Flight Pannel 页面可以同时核对 45 个遥测字段和 3 个操作项，无缺项；1440 px 宽屏无需切换数据分组。
- 单客户端 60 Hz 快速通道不会明显增加 KSP 帧时间；具体预算在实机基准后锁定。
- 手机竖屏所有触控目标不小于约 44 CSS px，无需横向滚动即可完成主要操作。
- Flight Recorder 连续记录 10 分钟以上，不因图表缩放重复发送全量数据。
- 断开网页或切换载具后，远程连续控制不会残留。
