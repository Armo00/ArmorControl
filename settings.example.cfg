ARMOR_CONTROL
{
    enabled = True
    autoStartServer = True
    bindAddress = 0.0.0.0
    port = 8765
    fastTelemetryHz = 30
    regularTelemetryHz = 5
    language = zh-CN
    // Empty disables authentication. Only use on a trusted local network.
    accessToken =
}
